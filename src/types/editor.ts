export enum ComponentType {
  Button = 'button',
  TextInput = 'textInput',
  NumberInput = 'numberInput',
  Select = 'select',
  Container = 'container',
  Table = 'table',
  Text = 'text',
}

export enum SidebarViewType {
  Inspector = 'inspector',
  Components = 'components',
}
